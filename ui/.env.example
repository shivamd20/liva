# API Configuration
# Set to 'true' to use remote API, leave empty or set to 'false' for local storage
VITE_USE_REMOTE_API=false

# API endpoint URL (defaults to http://localhost:8787/api/v1 if not set)
VITE_API_URL=http://localhost:8787/api/v1
